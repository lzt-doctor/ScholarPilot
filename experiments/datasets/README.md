# Demo dataset

`demo-v1` is a small synthetic dataset written for pipeline verification only. It is not a
scientific benchmark and must not be used to claim production retrieval quality. The queries and
chunks are committed so every reported demo result can be reproduced and inspected.
